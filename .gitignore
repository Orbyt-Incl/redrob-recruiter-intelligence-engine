# Large dataset (provided in the hackathon bundle)
candidates.jsonl
candidates.jsonl.gz

# Python
__pycache__/
*.pyc
.venv/
venv/

# Generated artifacts that exceed GitHub size limits — regenerate with precompute.py.
# Small artifacts (jd_requirements.json, *.parquet, *.npy) ARE committed; the FAISS index
# is tracked via Git LFS (.gitattributes). The embedding model is regenerated by precompute.
artifacts/embedding_model/
artifacts/_sample_emb.npz

# submission.csv IS committed as the reference deliverable (produced by rank.py).
